import {ReplaceTechnicalNamePipe} from './replace-technical-name.pipe';

describe('ReplaceTechnicalNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceTechnicalNamePipe();
    expect(pipe).toBeTruthy();
  });
});
