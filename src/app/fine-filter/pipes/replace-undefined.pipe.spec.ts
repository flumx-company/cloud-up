import {ReplaceUndefinedPipe} from './replace-undefined.pipe';

describe('ReplaceUndefinedPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceUndefinedPipe();
    expect(pipe).toBeTruthy();
  });
});
